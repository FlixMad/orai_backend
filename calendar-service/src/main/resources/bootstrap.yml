# application.yml보다 우선순위가 높은 설정 파일과 읽을 설정 파일 등록
spring:
  application:
    name: calendar-service
  cloud:
    config:
      uri: http://config-service:8888
      name: calendar-service
  profiles:
    active: default
